import { Kanit, Anton, Inter } from "next/font/google";

export const kanit = Kanit({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});

export const anton = Anton({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
});

export const inter = Inter({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});
